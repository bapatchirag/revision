---
/**
 * Starlight ThemeSelect override — intentionally empty.
 *
 * The site is dark-only: cipher.css declares identical values under :root,
 * [data-theme='light'] and [data-theme='dark'], so a toggle would be inert.
 */
---
